# RRR

Repository containing the `RRR.py` script.

Quick start

- Ensure you have Python 3 installed.
- Run the script:

```bash
python3 RRR.py
```

Notes

- CSV files are intentionally ignored by git via `.gitignore` (`*.csv`).
